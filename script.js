const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.main-nav');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('.experience-card').forEach(card=>card.classList.toggle('hide',filter!=='all'&&card.dataset.category!==filter))}));document.getElementById('trip-form').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const message=`Hola Río IA, soy ${data.get('name')}. Somos ${data.get('people')} personas y pensamos viajar en ${data.get('date')||'fecha a definir'}. ${data.get('idea')||'Queremos conocer opciones para el grupo'} Mi contacto es ${data.get('phone')}.`;navigator.clipboard?.writeText(message);const toast=document.querySelector('.toast');toast.textContent='Consulta preparada y copiada · Falta agregar el número oficial de WhatsApp';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),4500)});document.getElementById('year').textContent=new Date().getFullYear();
